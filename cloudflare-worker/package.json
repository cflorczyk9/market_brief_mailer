{
  "name": "market-brief-trigger",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that fires market_brief_mailer's GitHub workflows on a cron, as a third independent trigger alongside cron-job.org and GitHub Actions schedule.",
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
